# Отказ модерации Яндекс Игр — разбор и что сделано (2026-08-21)

Архив, который смотрел модератор, совпадает байт-в-байт с `dist/game.zip` и с
`public/` (sha256 `f1443ab9…e93e3e89`). Правим прямо в `public/`.

## Замечания и причины

| Пункт | Причина в проекте | Файл |
|---|---|---|
| 5.1.3 название | в игре «Угадай игру», в карточке «Угадай игру по иконке и скриншоту» | `index.html:6,20`, `promo/*-1-menu.png`, `img/hero.jpg`, `LISTING.md` |
| 2.7 возраст → 16+ | скрин Hunt: Showdown (кровь) | принято, спорить не будем |
| 1.10.1 обрезка | у `.screen--game` нет ландшафт-раскладки: 4-й ответ уезжает за экран | `css/style.css` (нет медиазапроса для игры) |
| 1.6.1.8 выделение iOS | `user-select:none` без `-webkit-` префикса — Safari ≤16.3 игнорирует | `css/style.css:56` |
| 8.4.2 ссылки | домены `steamgriddb.com` / `store.steampowered.com` в модалке «Об игре» | `index.html`, блок credits |
| 8.2.5 запрещённый контент | скриншот Schedule I (гроубокс) + найдена свастика в Wolfenstein II | `data/shots.json`, `img/shots/` |
| 3.5 права | иконки/скрины Steam — позиция в ответе модератору | — |
| формат описания | буллеты «•» и список режимов в поле «Об игре» | `LISTING.md` |
| 5.1.1.2 промо | pc-1/mobile-1 — меню, геймплея <70%, половина кадра пустая | `promo/`, `scripts/promo-shots.mjs` |

## Полный просмотр контента (1053 скриншота + 850 иконок, глазами)

### Обязательно убрать — прямые запреты
| Файл | Что |
|---|---|
| `schedule-i-1.jpg` | плантация конопли под фитолампами — претензия модератора |
| `wolfenstein-ii-the-new-colossus-1.jpg` | **свастика на нарукавной повязке крупным планом** |
| `postal-2-1.jpg` | игра целиком запрещённого толка |
| `mirror-1.jpg` | эротическая игра |

### Ссылки, домены и логотипы сторов (8.4.2)
`final-fantasy-xv-windows-edition-1.jpg` (лого STEAM + «Purchase on Steam»),
`frostpunk-1.jpg` (`www.frostpunkgame.com` + цены),
`dirt-rally-20-1.jpg` (лого Xbox/PS4/Steam/Windows/Oculus),
`warhammer-40-000-space-marine-2-1.jpg` (AMD/FOCUS),
`microsoft-flight-simulator-2020-…-1.jpg`, `watch-dogs-1.jpg`,
`insurgency-sandstorm-1.jpg`, `mortal-kombat-x-1.jpg` (цены),
`bright-memory-1.jpg` (NVIDIA RTX), `doom-doom-ii-1.jpg`

### Пресс-баджи и награды (чужие бренды, не геймплей)
`a-plague-tale-innocence-1.jpg`, `alan-wake-1.jpg`,
`life-is-strange-episode-1-1.jpg`, `outriders-1.jpg`, `pillars-of-eternity-1.jpg`

### Азартные игры
`balatro-1.jpg`, `governor-of-poker-3-1.jpg`, `liars-bar-1.jpg`,
`tabletop-simulator-1.jpg`, `raft-1.jpg`

### Жёсткая кровь / расчленёнка (решение за автором)
`chivalry-medieval-warfare-1.jpg`, `cruelty-squad-1.jpg`,
`dead-island-definitive-edition-1.jpg`, `dead-space-2-1.jpg`,
`dead-space-2008-1.jpg`, `hitman-2-silent-assassin-1.jpg`,
`payday-the-heist-1.jpg`, `surgeon-simulator-1.jpg`, `ultrakill-1.jpg`,
`world-war-z-1.jpg`, `serious-sam-3-bfe-1.jpg`,
`serious-sam-hd-the-first-encounter-1.jpg`, `call-of-duty-wwii-1.jpg`,
`prototype-1.jpg`

### Откровенное
`cyberpunk-2077-1.jpg` (обнажённая), `tower-of-fantasy-1.jpg`,
`vrchat-1.jpg`, `crush-crush-1.jpg`, `love-is-all-around-1.jpg`

### Иконки под замену
`company-of-heroes-legacy-edition.png` (офицер вермахта, Железный крест),
`return-to-castle-wolfenstein.png` (эмблема-орёл)

## Баг данных: 21 группа дублей (22 лишние записи)

Викторина показывает два неразличимых варианта ответа. 10 пар — точные тёзки
с id-суффиксом `-x` (баг `add-games.mjs`): `fear`/`fear-x`/`fear-x-x`,
`call-of-duty-ghosts`+`-x`, `medal-of-honor`+`-x`, `total-war-shogun-2`+`-x`,
`counter-strike-condition-zero`+`-x`, `call-of-duty-black-ops`+`-x`,
`call-of-duty-black-ops-ii`+`-x`, `call-of-duty-modern-warfare-2-2009`+`-x`,
`call-of-duty-modern-warfare-3-2011`+`-x`, `dark-messiah…`+`-x`,
`sid-meiers-civilization-iv`+`-x`.
Остальные — издания одной игры: GTA IV Complete / The Complete (это модератор
и заснял), BioShock / Remastered, BioShock 2 / Remastered, Fallout 3 / GOTY,
Mafia / Definitive, Mafia II Classic / Definitive, Sleeping Dogs / Definitive,
Ori / Definitive, Oblivion GOTY / GOTY Deluxe, AoE II Retired / Definitive.

## Что исправлено (21.08.2026)

Все правки — в `PATCH.md`, датасет чистится воспроизводимо скриптами
`scripts/moderation-clean.mjs` и `scripts/dedupe-games.mjs`.

| Пункт | Статус |
|---|---|
| 5.1.3 название | ✅ везде «Угадай игру по иконке и скриншоту»: `<title>`, `<h1>`, текст «Об игре», промо. Обложка и заставка уже были с полным названием |
| 2.7 возраст 16+ | ✅ принят, зафиксирован в `LISTING.md` |
| 1.10.1 обрезка | ✅ ландшафт-раскладка игрового экрана + компактный старт на низких окнах. Автотест: 0 проблем на 1280×800, 1000×630, 390×844, 844×390, 915×412 |
| 1.6.1.8 выделение iOS | ✅ `-webkit-user-select` + `-webkit-touch-callout` на body и всех кликабельных элементах |
| 8.4.2 ссылки | ✅ домены вырезаны из «Об игре»; в архиве не осталось ни одного URL, кроме SDK Яндекса. Кадры с логотипами сторов удалены |
| 8.2.5 контент | ✅ 5 игр и 22 кадра удалены; свастика в Wolfenstein II найдена и убрана |
| 3.5 права | ⚠️ позиция изложена в комментарии модератору — риск остаётся |
| формат описания | ✅ «Об игре» переписано сплошным текстом без маркеров |
| 5.1.1.2 промо | ✅ 8 кадров пересняты, все — геймплей |
| дубли ответов | ✅ 22 записи убраны, 1063 → 1036 игр |

Итог по датасету: **1036 игр, 827 иконок, 1010 скриншотов**, сирот и битых
ссылок нет. Архив — `dist/game.zip`, 33.0 МБ.

## Сплошной просмотр контента (08.09.2026)

Пересмотрены все 1010 скриншотов и 827 иконок контактными листами
(`npm run review`). Главное: раздел «Жёсткая кровь / расчленёнка (решение за
автором)» выше и раздел «Азартные игры» — **решение по ним не было принято, и
все 19 кадров оставались в игре**. Соседние списки (пресс-баджи, логотипы
сторов) тогда вычистили, а эти нет.

| Что | Сколько | Решение |
|---|---|---|
| кровь и расчленёнка | 12 кадров | ✅ удалены; +Khazan, найден сейчас |
| азартные игры | 6 кадров, 4 иконки | ✅ удалены; +Buckshot Roulette, найден сейчас |
| Железный крест на иконке Day of Defeat: Source | 1 иконка | ✅ удалена — то же, за что убрана иконка Company of Heroes |
| Mirror 2: Project X | игра целиком | ✅ удалена: сиквел удалённой `mirror`, та же эротика |
| `raft-1.jpg`, `payday-the-heist-1.jpg`, `prototype-1.jpg` | 3 кадра | оставлены: на кадрах ничего нет, в списки попали по названию игры |

Итог: **1035 игр, 823 иконки, 991 скриншот**, архив 33.1 МБ.

Списки вычищенного теперь живут в `scripts/moderation-list.mjs` — общие для
чистки и для проверки. У проверки был свой короткий список, и он уже разошёлся
с настоящим: стерёг три кадра из семнадцати.

На решение автора (граничное, не трогал): иконка The Binding of Isaac
(мультяшный обнажённый младенец), иконка DOOM 3 (пентаграмма), иконка Outlast 2
(перевёрнутый крест), игра POSTAL целиком, иконка Saints Row 2. Эмблема на кадре
Beholder рассмотрена вблизи — вымышленный знак, не свастика, кадр оставлен.

## Сверка по «Ядрён-Пустоши» (08.09.2026)

Прогнали проект по `YANDEX-MODERATION.md` соседней игры — обе части, замечания
модерации и собственные баги. Разбор и замеры в `PATCH.md`; сюда — только то,
что меняет картину по требованиям.

| Пункт | Что нашлось | Статус |
|---|---|---|
| 4.7 | сторож показа был 10 с — игра продолжалась под ещё идущим роликом | ✅ сторожа двухступенчатые, второй ставится в `onOpen` |
| 1.14 | у паузы площадки (`game_api_pause`) не было своего срока | ✅ сторож 180 с при видимой вкладке |
| 4.7 | `gameplayStart()` не звался после паузы на разборе — для площадки геймплей «не шёл» до конца партии | ✅ `resumeGameplay()` |
| 8.4.2 | **в архиве ехали 1837 адресов** `cdn2.steamgriddb.com` и `store.steampowered.com` — поля `source`/`author` в `icons.json` и `shots.json` | ✅ `pack.mjs` чистит данные на пути в архив; в репозитории поля остаются как доказательство по п. 3.5 |
| 8.3.3 | у иконки и обложки был альфа-канал (углы непрозрачные, но канал есть) | ✅ убран, `removeAlpha()` в генераторах |
| 1.10.1 | на окне высотой ≤360 px ряд «Лидерборды / Об игре» уходил за край (до 41 px) | ✅ медиазапрос, сплошной проход даёт 0 из 1830 |

Прежняя строка «в архиве не осталось ни одного URL» относилась только к коду и
разметке: проверка п. 8.4.2 смотрела `index.html`, CSS и JS и до данных не
доходила. Теперь смотрит и данные внутри собранного архива.

Реклама проверяется отдельно: `npm run check-ads` поднимает игру в iframe с
заглушкой SDK, которая умеет молчать, открыть ролик и не закрыть его, закрыть
без награды. Проверка испытана откатом — на коде до правки падает шагом п. 4.7.

Не закрыто прежнее: кегль и зоны нажатия по меркам соседнего проекта (16 px и
52 px). Требования Яндекса этих чисел не называют, а перекомпоновка ради них
рискует вернуть обрезку по п. 1.10.1.

## Третья сверка по «Ядрён-Пустоши» (15.09.2026)

В разборе соседней игры появилась третья часть — викторина «Знатоки Dota 2»,
почти целиком про вёрстку. Прогнали проект по её классам В1–В10. Подробности в
`PATCH.md`; сюда — только то, что меняет картину по требованиям.

| Пункт | Что нашлось | Статус |
|---|---|---|
| 1.10.1 | обрезка **зависит от длины названия**: на коротких проход был зелёным, на самых длинных (64 символа) четвёртый вариант уходил за край на 44px на 14 размерах | ✅ свой ярус для узких и низких окон, варианты в два столбца |
| 1.10.1 | у `.controls` и `.modal__scroll` заданный `min-height` снимал защиту «не ниже содержимого» — коробку сминало, а содержимое вылезало уже из неё; текст «Об игре» уезжал за край на 41 размере | ✅ `min-height: auto` / `min-height: 0` по месту |
| 1.10.1 | квадратное окно (320x320, 360x360) по спецификации **портрет**, и ярус с `orientation: landscape` мимо него проходил — картинка сжималась до 4px | ✅ ярус переписан по размерам, без `orientation` |
| 1.8 | на 320px подпись «50/50» схлопывалась в ноль, а бейдж «за рекламу» оставался: кнопка не сообщала, что делает | ✅ подпись не сжимается, бейдж переносится внутри чипа |
| 1.6.1.8 | запрет выделения стоял на списке из десяти классов — разбор ответа, подписи режимов и текст модалок в него не попали | ✅ запрет на `*` |
| 1.10.2 | на мобиле всё, размеченное в `vh`, считалось от **большой** области просмотра и было крупнее кадра; прокручиваемым спискам не хватало `overscroll-behavior: contain` | ✅ `dvh` рядом с `vh`, `contain` на списках |
| — | `:hover` залипал на касании: ответив, игрок видел следующий вопрос с уже обведённым вариантом — прямая подсказка | ✅ все 10 правил под `@media (hover: hover)` |
| — | **облачный рекорд стирался при сбое чтения**: `loadBest` клал нули, `saveBest` писал ими поверх — один таймаут `getData` уносил рекорды четырёх таблиц | ✅ не прочитали — не пишем; `check-leaderboard` вырос до 14 проверок |

Главное не про вёрстку, а про измеритель. Сплошной проход брал **тот вопрос,
который выпал**, смотрел только на кадр и искал прокрутку у документа. На том же
коде: старая проверка — **0 из 1830**, починенная — **112 из 2135**. После правок
— **0 из 2135**.

Не закрыто прежнее: кегль и зоны нажатия по меркам соседнего проекта (16px и
52px). Глобальной ужимки у нас нет, и класс В5 («кегль после ужимки») мимо —
вёрстка держится ярусами, то есть правильной стороной развилки.

## Сверка после редизайна игрового экрана (17.09.2026)

Игровой экран переделан под рабочий стол (ярлык с заклеенной подписью,
контекстное меню вместо кнопок ответа), счёт вернулся к сотням за ответ.
Прогнали по всем автоматическим проверкам заново — переделка вёрстки ровно то
место, где модерация нас уже ловила.

| Проверка | Итог |
|---|---|
| `npm run verify` — 9 пунктов отказа | 9 из 9, провалов нет |
| `npm run sweep` — сплошной проход по размерам | 0 из 2135 |
| `npm run check-ads` — реклама, п. 4.5/4.7/1.14 | 11 из 11 |
| `npm run check-audio` — звук, п. 1.3 | 10 из 10 |
| `npm run check-leaderboard` — рекорды | 14 из 14 |
| `npm run check-blur` — замыленные названия на кадрах | 13 из 13 |
| `npm run build` + `npm run pack` — данные и архив | целостны, 33.1 МБ |
| стресс-тест, 100 партий | замечаний по игре нет; 2 прогона сорвались по таймауту CDP самого puppeteer |

Что поправлено по ходу сверки:

- **п. 5.1.1.2 промо**: 8 кадров и оба ролика показывали **старый** игровой
  экран — сняты 08.09, до переделки. Пересняты; в кадре теперь и ярлык, и
  «+100» за ответ.
- **контраст**: белая подпись переименованного ярлыка лежит на заливке
  `--accent-fill`, а её верхний стоп `#4f6fe6` давал 4.42:1 — под порогом 4.5:1
  для мелкого текста. Стоп затемнён до `#4b6ade` (4.76:1).
- **кегль**: шапка контекстного меню была 11px — поднята до 12px.

Осталось незакрытым:

- **п. 1.8, зоны нажатия в ландшафте**: на телефоне лёжа (`max-height: 560px`)
  чипы HUD дают 34–36px вместо 44 — подсказка 234x35, звук и «домой» по 36x36.
  Правило живёт с первой загрузки проекта (`5640120`), редизайн его не менял, и
  прошлые сверки его не заметили: проверка смотрела обрезку, а не зоны. Чинить
  надо аккуратно — лишние 10px высоты в ландшафте это ровно тот риск вернуть
  п. 1.10.1, о котором сказано выше.
- **сторонний чекер** прогнан отдельно, см. раздел ниже.
- **п. 3.5 права** — как и было, позиция изложена в комментарии модератору.

## Новое меню и палитра (17.09.2026)

Стартовый экран переделан в «рабочий стол» (окно с ярлыками-режимами, панель
задач), «Об игре» — в окно «Свойства», палитра — «циан + янтарь» при прежней
синей базе. Что это значит для требований:

| Пункт | Проверено |
|---|---|
| 5.1.3 название | `<h1>` с полным названием остался, `verify` зелёный |
| 1.8 зоны нажатия | все кнопки стартового экрана ≥ 44px на 7 размерах от 320x320; на низких окнах — накладка `::after` |
| 1.10.1 обрезка | `sweep` 0 из 2135 после двух правок под 320px |
| 1.6.1.8 выделение | запрет стоит на `*`, `verify` зелёный |
| контраст | циан на карточке 8.8:1, тёмный текст на янтаре 11.3:1, `--muted` 5.75:1 |
| 8.4.2 | в «Свойствах» нет адресов и доменов; «Яндекс Игры» — название площадки, не ссылка |

Часы на панели задач и «Свойства: Угадай игру.exe» — оформление внутри
игры, элементов площадки и рекламы они не имитируют.

Обои стартового экрана (`img/desk-land.jpg`, `img/desk-port.jpg`) — свои,
собраны скриптом из иконок того же набора, что играется, размыты и затемнены;
названий, чужих логотипов сторов и адресов на них нет.

## Время до ready() и загрузка картинок (17.09.2026)

По опыту соседней викторины проверили скорость загрузки на эмуляции 3G. Сами
картинки лёгкие (13–19 КБ, 0.3–0.5 с), а до `LoadingAPI.ready()` игра ждала
три сырых JSON на 765 КБ: Fast 3G — 6.7 с, Slow 3G — 26 с. Окно п. 1.19 —
90 с, формально запас был, но модератор на слабой сети видел бы прелоадер
дольше, чем игру.

| Что | Было | Стало |
|---|---|---|
| данные до `ready()` | 3 файла, 765 КБ | `data/bundle.json`, 137 КБ (gzip 38) |
| `ready()` на Fast 3G | ≥ 6.7 с | 2.6 с |
| `ready()` на Slow 3G | ≥ 26 с | 9.8 с |
| «Играть» → первый кадр | 330–1500 мс | 24–38 мс (кадр прогрет в меню) |

Заодно по п. 8.4.2: сырые данные с адресами источников в архив больше не едут
вовсе — раньше из них вычищали поля по одному. `verify` теперь требует в
архиве `bundle.json` и отсутствие `games.json` / `icons.json` / `shots.json`.
Подробности — в `PATCH.md`.

## Музыка заменена (17.09.2026)

Фоновый трек теперь «Dumus» автора pauliuw, лицензия **CC0 1.0** — набор
«Music Loops» на OpenGameArt.org. Указание автора не требуется, коммерческое
использование разрешено; автор и лицензия всё равно записаны в титрах игры и в
тегах самого файла `audio/theme.mp3` (`copyright`, `comment`), как у прежнего
трека, — чтобы модерации это было видно прямо из файла (п. 3.5).

## Читеры в лидербордах: что можно из консоли и что мы с этим сделали (17.09.2026)

В двух соседних играх в таблицах появились игроки с неправдоподобными
результатами. Разобрали, как это делается, и закрыли то, что можно закрыть
без сервера.

### Как ставят себе рекорд

Игра целиком выполняется в браузере игрока, и всё, что умеет вызывать её код,
игрок может вызвать сам:

1. открыть игру на площадке, войти в аккаунт Яндекса (без входа результаты не
   принимаются);
2. F12 → вкладка Console → выбрать фрейм игры в списке контекстов;
3. одна строка — тот же вызов SDK, которым пользуется игра:
   `YaGames.init().then(s => s.leaderboards.setScore('leadtotal', 123456))`.
   Имена таблиц видны в коде игры;
4. площадка записывает число как результат. Партии не было, проверять некому.

Облачное сохранение (`player.setData`) устроено так же: это хранилище, а не
сервер с логикой — оно пишет то, что прислали. Личный рекорд подделывается той
же строкой. Второй способ — бот, который отвечает сам: ответ всегда есть у
клиента (картинка, варианты, разбор), у нас он даже в имени файла картинки.

По данным таблицы читера не доказать: очки, ранг и имя — всё, что она хранит.
Косвенные признаки: результат выше потолка правил, круглые числа (999999,
100000), одинаковые очки у нескольких игроков, партии, не укладывающиеся во
время.

### Что сделано

Штатное поле `extraData` лидерборда превращает голое число в проверяемую запись.

| Мера | Где | Что даёт |
|---|---|---|
| **Подпись партии.** Вместе с очками уходит сводка `1\|вопросов\|верных\|секунд\|режим\|тип\|подпись`; подпись — хэш от очков, сводки и соли в коде | `sdk.js`: `signExtraData`, `submitScore` | запись из консоли подписи не имеет |
| **Фильтр при показе.** Топ-5 в меню, окно лидербордов, таблица на экране итогов показывают только строки с верной подписью и правдоподобными цифрами: очков не больше «верных × 200», верных не больше вопросов, секунд не меньше вопроса | `sdk.js`: `verifyEntry`, `topScores` | консоль, подделка и невозможные цифры не видны никому |
| **Своя строка видна всегда** — с пометкой «не подтверждено», если подпись не сошлась | `main.js`: `fillBoardList` | честный игрок пометки не увидит никогда, читер увидит |
| **Потолок скорости.** Партия быстрее секунды на вопрос не отправляется | `sdk.js`: `plausible` | бот, гоняющий наш интерфейс на скорости |
| **«Рядом с тобой».** Кроме топа — соседи игрока по таблице (`quantityAround`) | `main.js`: `openBoards` | читер в топе не портит впечатление: игрок видит своё окружение |

Проверки — `npm run check-leaderboard`, 21 штука: отправка несёт подпись;
25 вопросов за 10 секунд не уходят, за 60 — уходят; в таблице из консольной,
подделанной, невозможной, честной и своей строки видны только честная и своя;
своя помечена; из соседей показан только подписанный; то же — в окне
лидербордов глазами игрока.

### Границы

Подпись считается в клиенте, соль лежит в коде: кто разберёт исходник, тот её
повторит. Бот, играющий с человеческой скоростью, по данным неотличим от
сильного игрока. Это барьер против консоли и готовых скриптов — того, что
видно в соседних играх, — а не против разработчика. Сильнее без своего
сервера не сделать: у площадки серверной логики нет.

На площадке фальшивые записи остаются — мы их только не показываем; но
таблицу показывает только наша игра. Если таблица засорена так, что мешает,
её можно пересоздать в консоли разработчика под тем же именем — это единственная
чистка, которую даёт площадка.

Тот же механизм переносится в две другие игры без изменений: `sdk.js` и
рисовалка таблицы.

## Сторонний чекер, второй прогон (17.09.2026)

Тот же Yandex Games Debug Checker 1.1.0, распакован в `E:/yandex-games-debug-checker`,
запуск: `node scripts/run-debugchecker.mjs E:/yandex-games-debug-checker`.

**0 FAIL, 72 PASS, 12 WARN, 2 NOT VERIFIED (всего 86).** Разбор всех WARN:

| Что предупреждает | Разбор |
|---|---|
| звук на рекламе (п. 4.7), пауза игры на рекламе (п. 4.7), AudioContext suspend/resume | эвристики по исходнику: ищут `suspend()` прямо в `onOpen`. У нас это делает свой обработчик паузы, и `check-ads` подтверждает фактом: под рекламой контекст suspended, громкость 0.04, геймплей остановлен |
| `LoadingAPI.ready()` слишком рано (п. 1.19), ready в том же кадре | `sdk.ready()` стоит последней строкой `boot()`, уже после `show(el.start)`, — игра к этому моменту играбельна |
| ввод до `ready()` (п. 1.19) | явного гейта `inputEnabled` нет, но до `ready()` виден только экран загрузки: стартовый показывается строкой выше, в том же тике. Рантайм-строки «ВВОД ПРИНЯТ ДО ready()» чекер не напечатал ни разу |
| мат в тексте (п. 8.2.4) | ложное срабатывание на стем внутри слова, как и 22.08 |
| `setLeaderboardScore` / `getLeaderboardEntries` устарели, имя лидерборда | `callLeaderboard` зовёт современные `setScore`/`getEntries` первыми, старые — только как запасные. Имена (`leadtotal` и остальные) состоят из букв, но передаются переменной, и эвристика их не видит |
| `touch-action: none` | намеренно не ставим: сломает прокрутку модалок |
| множественные ошибки в консоли (рек. 6.4) | `No parent to post message` от самого SDK площадки: вне её iframe ему некому слать `postMessage`. За партию 15 необработанных отказов, и все рождаются внутри `sdk.js` площадки — наружу эти промисы не отдаются, перехватить их из игры нельзя |

Заодно закрыт свой класс той же беды: `LoadingAPI.ready()`, метки `GameplayAPI` и
запуск роликов звались без обработки отказа (синхронный `try` промис не ловит), а
`withTimeout` бросал проигравший гонку промис без `catch`. Теперь такие вызовы идут
через `fireAndForget`, поздний отказ гасится. На число ошибок в консоли это не
влияет — они не наши, — но свои вызовы больше не могут оставить висящий отказ.

## Сторонний чекер (22.08.2026)

Прогнали Yandex Games Debug Checker v1.1.0
(github.com/Nioris/yandex-games-debug-checker) — `node scripts/run-debugchecker.mjs`.

**0 FAIL, 63–73 PASS, 11 WARN.** Первый прогон дал 3 FAIL:

- `SDK script tag` — настоящий: подключали абсолютный `https://yandex.ru/games/sdk/v2`
  вместо документированного `/sdk.js` (п. 1.1). Исправлено.
- `YaGames.init()` и `LoadingAPI.ready()` — ложные: чекер читает только файлы из
  тегов `<script src>` и не идёт по ES-импортам, а у нас четыре файла из пяти
  подключены импортами. В прогоне через прокси модули перечисляются тегами явно.

Из предупреждений по делу оказались три и все исправлены: перерисовка канваса по
`ResizeObserver` вместо одного `window.resize`, `overflow: hidden` на `html`,
зоны нажатия 44x44 (п. 1.8). Остальные 11 — неприменимы: заглушение звука на
рекламе (звука в игре нет), «мат» (стем внутри слова «тр*ебу*ет» в комментарии),
устаревшие методы лидербордов (наш helper зовёт современные первыми), имя
лидерборда передаётся переменной, «канвас не во весь экран» (у нас DOM-игра),
`touch-action: none` (сломал бы прокрутку модалок).
